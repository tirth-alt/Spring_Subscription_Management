package com.example.subscription_platform.model;

/**
 * Enum representing user roles in the system.
 * - USER: Regular user with access to subscription management
 * - ADMIN: Administrator with full access including analytics and plan
 * management
 */
public enum Role {
    USER,
    ADMIN
}
